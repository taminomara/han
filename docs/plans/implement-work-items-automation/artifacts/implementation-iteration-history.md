# Implementation Iteration History: implement-work-items Driver Automation

Round-by-round record of how the implementation plan evolved. Committed decisions live in [implementation-decision-log.md](implementation-decision-log.md); the primary plan lives in [../feature-implementation-plan.md](../feature-implementation-plan.md).

## R1: Parallel specialist review

- **Specialists engaged:** junior-developer, on-call-engineer, test-engineer, adversarial-security-analyst (project-manager: synthesis only, Step 8 — not called this round; the spec-maturity gate did not trip).
- **New input provided:** initial feature specification + its decision-log/team-findings/feature-technical-notes (T1, T2) + `.discovery-notes.md`.
- **Claim ledger:** (all Evidenced with `file:line`; none Anecdotal; none Disputed)

  | Category | Claim (consolidated) | State | Maturity |
  |---|---|---|---|
  | Security (SEC-001,005) | Newline/control-char in an item ID, decision text, or iteration marker forges a commit trailer (SEC-001) or a grammar-valid ledger line (SEC-005) — the two prior bug classes reappearing at D5/D10/D11's new sinks. Guard: apply the existing `require_single_line` (`write-run-record.sh:33-40`) to every value reaching a commit message/trailer or an appended record line; regex-validate any item ID reaching a trailer. | Evidenced (reproduced, git 2.54.0) | plan-level |
  | Security (SEC-002) | `git add` of item paths admits option-injection (`--all`) or pathspec-magic (`:(exclude)`), escaping D5's narrow-staging. Guard: `--` before pathspecs, `:(literal)` or refuse leading `-`/`:`, reuse `normalize_path` + `../`/absolute refusal to confine. | Evidenced | plan-level |
  | Security (SEC-004) | New D3 fetch / D5 branch commands must inherit the sibling scripts' `--end-of-options`/`--` ref pinning; validate branch name via `git check-ref-format --branch`. | Evidenced | plan-level |
  | Security (SEC-003) | The working-tree ledger fallback (`scan-run-history.sh:146-152`) must not satisfy resume-critical reads once D10/D11 state lives there — **honors T1** (preserve the `:138-140` traversal refusal; gate resume-critical reconstruction on `git show HEAD:` only). | Evidenced | plan-level |
  | Resilience (OCE-001) | Write-then-commit has no rollback → a hook-rejected commit leaves the record file mutated but uncommitted (torn state). Fix: rollback reusing `write_atomic` to restore pre-write content + `git reset` the staged path on commit failure. | Evidenced | plan-level (blocks ship) |
  | Resilience (OCE-002) | `cmd_log` has no duplicate guard → writer dies after committing `start-of-item` before returning the baseline ref → duplicate baseline commit on re-entry. Fix: `grep -Fxq` existence check (as `cmd_block:147`); re-emit the existing baseline ref idempotently. | Evidenced | plan-level (blocks ship) |
  | Resilience (OCE-003) | Must distinguish hook-rejection from clean no-op. Fix: `git diff --cached --quiet -- <rec>` before committing — nothing staged ⇒ idempotent no-op; staged + non-zero commit ⇒ unambiguous rejection ⇒ resumable stop. | Evidenced | plan-level (blocks ship) |
  | Resilience (OCE-004) | Self-fetch needs a bounded wall-clock deadline; a slow/offline fetch folds into `fetch-status: failed`. Fix: `timeout <N>s git fetch`. | Evidenced | plan-level (blocks ship) |
  | Resilience (OCE-005) | Classify resume-vs-fresh BEFORE the fetch so no fetch runs on resume (scan → classify → fetch only if fresh + base unresolved). | Evidenced | plan-level |
  | Resilience (OCE-006) | Marker-last ordering (D14) must be enforced by construction across the two-actor split; optionally the tool verifies the backing commit resolves before writing a terminal marker. Reader default-deny is the crash-time backstop, not the only defense. | Evidenced (honors D14/T2) | plan-level |
  | Resilience (OCE-007) | Stage only the exact record file (`git add -- "$rec"`); assert tree state only against that path, so a formatter-on-save edit is neither swept in nor a false halt. | Evidenced | plan-level |
  | Resilience (OCE-008) | After a bookkeeping commit, read the commit's own trailers back and assert they survived (a trailer-stripping hook otherwise silently breaks resume). | Evidenced | plan-level |
  | Resilience (OCE-009) | Existing `die`-to-stderr surface is adequate; no new telemetry (recorded as YAGNI-safe). | Evidenced | plan-level |
  | Structure (JD-3) | Return-contract ownership under the actor split: the **required return format** lives in the sub-agent-facing reference; the **driver-facing** reference holds only the headers it parses + halt conditions. The current contract files already separate these two halves. | Evidenced | plan-level |
  | Structure (JD-2) | "8→2" is imprecise: the 3-file hand-off collapses to 2 actor-named references; the other 5 are renamed and some fold in (no-output-completion shrinks post-F14; re-grounding is gutted by D4). The plan must enumerate the true end-state file list. | Evidenced | plan-level |
  | Structure (JD-6) | `re-grounding` step 4 (reload-instructions) cannot be scripted; it stays a residual driver step after D4 moves steps 1-3 into tooling. | Evidenced | plan-level |
  | Staging (JD-5) | The opening co-commit (record + planning artifacts) is an explicit exception to D5's narrow-staging; per-item bookkeeping commits stage only the record file. | Evidenced | plan-level (refines D5) |
  | Sequencing (JD-1, test-engineer) | Dogfooding hazard: build + green the script/grammar/harness changes BEFORE the SKILL.md driver-prose changes; the building run stays on current logic (no mid-run hot-swap); prefer a single continuous dogfood session; rollback = `git revert` (no CI). | Evidenced | plan-level |
  | Testing (T1-T17, T9, S1) | Writer harness needs a per-case git sandbox (was "no repo"); D8/T2 round-trip + negative-removal of the `no-commit-done`/`done-no-commit` tokens; D3 via a local bare "remote" with a controlled-hang double (not a real sleep); **T9**: existing `scan-run-history.test.sh` case 17 asserts the working-tree fallback DOES populate items — must be updated to T1's committed-only boundary; **S1**: D7 (reviewer judges scope) has no script entry point → end-to-end only. Full detail in [test-plan.md](test-plan.md). | Evidenced | plan-level |
  | Standards (JD-4) | Body is over the 500-line ceiling (532); the spec's Out of Scope already defers de-bloating, so over-500-after-this-change is tolerated. This change is expected net-neutral-to-negative on line count. | Evidenced (spec Out of Scope) | plan-level |
  | Standards (JD-8) | `allowed-tools` uses `Bash(git *)` (wildcard); moving git into scripts shrinks the driver's direct git surface and requires permitting script execution. Narrow to observed usage; do not speculatively enumerate git verbs. | Evidenced | plan-level (→ open item / handoff) |

- **Open Questions raised (all resolved by evidence this round):**
  - **OQ-1 (dogfooding build/verify sequence)** → evidence: stated build order + single-session dogfood + `git revert` rollback (JD-1, test-engineer two-phase, both retrospectives).
  - **OQ-2 (return-contract ownership)** → evidence: format in the sub-agent-facing reference, parse/halt in the driver-facing reference (the split already exists within `build-report-contract.md` / `review-verdict-contract.md`).
  - **OQ-3 (reference end-state file list)** → evidence: enumerate the ~5-6 file end state (2 hand-off + renamed record-contract/foreground/human-review; fold no-output-completion + re-grounding-residual).
  - **OQ-4 (reload-instructions home after D4)** → evidence: residual driver step in the driver-facing reference / a one-line SKILL.md step.
  - **OQ-5 (opening co-commit vs narrow-staging)** → evidence: opening commit is an explicit exception; stages the record + enumerated planning-artifact paths.
  - **OQ-6 (branch-creation idempotency, on-call OQ1)** → evidence: the tool's setup subcommand is idempotent create-or-switch.
  - **OQ-7 (trailer-stripping hooks, on-call OQ2)** → resolved regardless by OCE-008 (post-commit trailer readback).
  - **OQ-8 (allowed-tools narrowing)** → deferred to an open item + a plugin-builder/skill-authoring handoff; narrow to observed usage, no speculative enumeration.
- **Spec-maturity tags:** plan-level: all ~21 consolidated claims. spec-level: 0. T#-contradiction: 0 (SEC-003 and the OCE ordering findings HONOR T1/T2/D14). **Gate did NOT trip** (needs ≥2 T#-contradictions by ≥2 specialists, or ≥5 spec-level by ≥3 specialists).
- **Resolution source:** evidence for OQ-1..OQ-7; deferred-to-open-item for OQ-8. No junior-developer reframing round or user escalation was required (junior-developer's own R1 findings supplied the reframing).
- **Decisions produced:** D-1 (write_and_commit paved path — OCE-001/003/007/008), D-2 (idempotent writes + baseline re-emit — OCE-002, OQ-6), D-3 (input guards at every new git sink — SEC-001/002/004/005), D-4 (bounded single-attempt self-fetch + resume-before-fetch — OCE-004/005), D-5 (resume-critical reads gate on committed artifacts; marker-last by construction — SEC-003, OCE-006, T1/T2/D14), D-6 (retire `no-commit-done`; audit done via confirmation-record commit — T2/F14), D-7 (build/verify sequence + single-session dogfood + `git revert` rollback — OQ-1), D-8 (over-500 tolerated, trivial — JD-4), D-9 (retain-all per-round records, trivial — JD-9), D-10 (`allowed-tools` narrowed to observed usage, trivial — JD-8/OQ-8), D-11 (two-actor reference split + return-format ownership + enumerated end-state — OQ-2/3/4, JD-2/3/6), D-12 (opening co-commit narrow-staging exception — OQ-5, JD-5).
- **Changed in plan:** all sections written this round (the plan is authored at synthesis from R1): Source Specification, Outcome, Context, Team Composition and Participation, Implementation Approach, Decomposition and Sequencing, Testing Strategy, Security Posture, On-Call Resilience Posture (Operational Readiness confirmed omitted — no operational surface), Definition of Done, Specialist Handoffs for Implementation, Deferred (YAGNI), Open Items (OI-1 inherited, OI-2 allowed-tools added).
- **Project-manager next-step recommendation:** Go to synthesis. One round sufficed: findings are unanimous in direction (extend existing guard primitives; fold into one `write_and_commit` helper), fully evidenced, with no disputed claims, no spec-level gaps, and no T#-contradictions.
