# Discovery Notes: implement-work-items Driver Automation implementation

Single source of truth for project context. Specialists: read this first; do not re-grep for what is here. Search further only for what your domain needs that this does not cover.

## What is being built

A change to an existing skill **in this repo**: `han-coding/skills/implement-work-items/`. Not a product feature — the "system" is a Claude Code skill (a driver agent + its bash scripts + its markdown references). The implementation touches known files (enumerated below). Ground truth for "what exists today" is the skill's own files.

## Tech stack

- **Skill definition:** `han-coding/skills/implement-work-items/SKILL.md` — markdown with YAML frontmatter (`name`, `description`, `argument-hint`, `allowed-tools`), body is numbered steps. **Body ceiling: 500 lines** (skill-authoring standard, below). Currently **532 lines — already over** (the second-run feedback flagged this); this feature removes driver prose, which should help, though de-bloating is a separately-deferred feature.
- **Scripts:** 3 bash scripts + 3 `*.test.sh` harnesses under `scripts/`. Convention: **pure bash, `set -u`, `set -o pipefail`; no jq/python3 in `write-run-record.sh`**; atomic temp-file-then-`mv` writes; fail-loud. Invoked from SKILL.md as `${CLAUDE_SKILL_DIR}/scripts/<name>.sh`.
  - `detect-driver-context.sh` — git state, uncommitted files, base-candidate ahead/behind, manifest-inferred verify commands. **Deliberately read-only today** (D3 changes this).
  - `scan-run-history.sh` — classifies invocation (fresh/resume/refuse/no-base), reconstructs per-item state from the committed ledger; maps `no-commit-done`→`done-no-commit` (D8/F14 removes this); has a **working-tree ledger fallback** (T1/F2 flags it).
  - `write-run-record.sh` — append-only record writer with `init`/`log`/`block`; **never runs git today** (D5 changes this); has `require_single_line` guards against newline/control-char injection (the pattern D5's git-arg construction must extend).
- **References:** 8 markdown files under `references/` (D6 consolidates to 2 actor-named + renames the rest). Named in the spec's D6/Coordinations.
- **Tests:** harnesses run directly, e.g. `write-run-record.test.sh <path-to-write-run-record.sh>` (pure bash, tmp-dir sandbox). **No project-wide test suite and no CI** (`.github/workflows` absent) — the second-run feedback confirms runs use scope-check-only mode. The `*.test.sh` harnesses are the only automated gate; resume/integrity correctness rests on them.
- **Long-form doc:** `docs/skills/han-coding/implement-work-items.md` — must be updated in the same change (CONTRIBUTING doc-sync rule; every skill has a long-form doc).

## Standards / ADRs the change must respect

- **Skill-authoring guidance:** `han-plugin-builder/skills/guidance/references/skill-building-guidance/` — the "coding standards" for this markdown-heavy change. Relevant docs: `progressive-disclosure.md` (500-line ceiling; deterministic ops → `scripts/`), `hardening-fuzzy-vs-deterministic.md` (which steps become scripts), `skill-reference-files.md`, `allowed-tools-bash-permissions.md` (per-prefix, not wildcards), `script-execution-instructions.md`, `naming-conventions.md` (bears on D6's rename), `documentation-maintenance.md` (long-form doc sync).
- **CONTRIBUTING.md:** `han-coding` is the right plugin; body uses `${CLAUDE_SKILL_DIR}` script paths; `allowed-tools` must match actual usage; context-injection `!`command`` must stay simple (complex ops in scripts); YAGNI applies to docs.
- **No ADRs** under `docs/adr/` for this skill. No coding-standards dir beyond the plugin-building guidance.

## Code touch points (the file inventory)

- `SKILL.md` — remove moved prose (per-item commit orchestration, clean-tree assertions, detect→fetch→re-detect dance, expected-paths validation/scope/coherence keying, improvised re-grounding, no-commit-done handling); route to the 2 references + tooling.
- `scripts/write-run-record.sh` (+ `.test.sh`) — grows to own dir+branch creation, commits with trailers, tree-state assert, return the baseline ref; new blocks for decisions + iterations.
- `scripts/detect-driver-context.sh` (+ `.test.sh`) — self-fetch with deadline, resume-awareness (no fetch on resume), expanded base candidates (`dev`/`develop`/`development`/`trunk`).
- `scripts/scan-run-history.sh` (+ `.test.sh`) — drop `no-commit-done`; extend reconstruction (decisions, fix-round count); audit `done` resolves via confirmation-record commit (T2); reconsider the working-tree fallback (T1).
- `references/` — 8 files → 2 actor-named (driver-facing hand-off, sub-agent-facing guidance) + consistent renames of the rest (durable-record-protocol, no-output-completion, re-grounding-routine, foreground-handoff, human-review-capture).
- `docs/skills/han-coding/implement-work-items.md` — long-form doc update.

## Recent activity (churn)

The entire machinery being changed was built/hardened in the last 90 days: the writer (`6d5f2b0`), the block-aware scanner (`c4af8b0`), the four-trailer contract (`3fb8140`), the commit-every-iteration loop (`2a45c1a`), the done-entry-as-sole-authority (`09f0ff2`), the `no-commit-done` entry (`f693c1e`), base ahead/behind detection (`8bb7562`), commit-or-stash at startup (`bcfd395`). The git history is rich, current precedent — read it for how each mechanic was introduced.

## Gaps (searched, not found)

- No CI / automated test gate (`.github/workflows` absent). No project-wide test runner. No ADRs for this skill. No shellcheck config found (bash lint is convention, not enforced).
- Open at spec time: **OI-1** — whether the detector's self-fetch should be suppressible offline (non-blocking).
- Committed mechanics the plan must honor (do not re-debate): **T1** (resume-critical state lives in committed artifacts, not the working-tree fallback), **T2** (audit completion resolves against its confirmation-record commit).
