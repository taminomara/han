# Implementation Iteration History: Resume, Halt Recovery, and Re-Grounding for the Work-Item Driver

<!--
Round-by-round record of the plan-implementation team. Committed decisions live in
[implementation-decision-log.md](implementation-decision-log.md); the primary plan is
[../feature-implementation-plan.md](../feature-implementation-plan.md). `Decisions produced:`
and `Changed in plan:` are backfilled during the project-manager's synthesis (Step 8).
-->

## R1: Parallel specialist review

- **Specialists engaged:** `han-core:junior-developer`, `han-core:on-call-engineer`, `han-core:structural-analyst`, `han-core:behavioral-analyst`. (`han-core:project-manager` reserved for synthesis; not called this round — the spec-maturity gate did not trip.)
- **New input provided:** the reconciled feature specification, its decision log (D1–D21), technical notes (T1, T2), team findings, the just-completed sync-review findings (review-findings.md F1–F16), the current skill (`SKILL.md` + 5 references + `scripts/detect-driver-context.sh`), the operator doc, the skill-authoring guidance, and `.discovery-notes.md`.
- **Claim ledger:**

  | # | Claim | Supporting specialists | State |
  |---|---|---|---|
  | CL-1 | Record form = **committed ledger file**, not an empty marker commit | junior (JD-002/003), behavioral (B9/B10/B12), on-call (OCE-009) | Evidenced (T1 hook-conformance; no-empty-commit hooks; commit-message mutation breaks greppable message markers) |
  | CL-2 | Decompose into 2 new reference files (`re-grounding-routine.md`, `durable-record-protocol.md`) + 1 new sibling script (`scan-run-history.sh`) | junior (JD-001), structural (S1/S2/S3/S7) | Evidenced (500-line SKILL.md ceiling; hardening rule; script needs a path param + post-1.2 timing that `detect-driver-context.sh` cannot take) |
  | CL-3 | Re-grounding = one core routine + per-site wrapping; cross-session reconstructs `state.json` from the record, never reads the stale gitignored file as authority | junior (JD-005/006), structural (S1), on-call (OCE-004) | Evidenced (D9; D13; D19 go-ahead is cross-session only) |
  | CL-4 | Ledger lives at `.implement-work-items/progress.md` via a `!progress.md` un-ignore, so existing `.implement-work-items/` exclusions cover it | behavioral (B10) vs on-call (OCE-007)/structural (S6) who assumed an outside path | Disputed → resolved by evidence toward inside-placement; keep the OCE-007 exclusion-site checklist as a verification step |
  | CL-5 | Opening entry co-committed **atomically** with the planning-artifacts commit (else a failed opening entry leaves a branch refused as a foreign base) | on-call (OCE-001) | Evidenced (contradicts edge case "opening-entry fails → fresh run", spec line 104) |
  | CL-6 | Forward-reconcile scoped to **AFK items only**; interrupted foreground items route to re-verify/re-review first; the positive-classification test reuses the existing scope computation to reject a foreign commit in range | on-call (OCE-002), behavioral (B6) | Evidenced (spec's own D16 reasoning "an unattended build never commits", line 170; D11 default-deny; foreground skills commit pre-gate) |
  | CL-7 | Bookkeeping-commit failure gets a **physically distinct** surface-and-stop path, never the Step 3.4 code fix loop; one shared "record a bookkeeping entry" routine | on-call (OCE-003) | Evidenced (D20; SKILL.md:282-291 routes commit-hook failure into the fix loop) |
  | CL-8 | Commit-boundary self-check = **unconditional re-Read** of both done paths (not a "detect truncation" judgment a compacted driver cannot make); one shared "record the item done" routine forking output vs no-output | on-call (OCE-005) | Evidenced (D18; hardening rule; review-sync F15 spans two files) |
  | CL-9 | Done-entry reference is **positional** (no stored hash); the code commit strictly precedes the done entry; the display reference is computed at report time | on-call (OCE-006), behavioral (B2/B3) | Evidenced (T1 line 11 "real commits rather than stored hashes"; D2 rejects stored hashes) |
  | CL-10 | Record marker = collision-resistant **namespaced git trailer** keyed on run identity, matched exactly | on-call (OCE-008), behavioral (B12) | Evidenced (the entire D10 fresh/resume/refuse safety rests on the match) |
  | CL-11 | Add the **item id** to the driver's code commits (message trailer) for forward-reconcile positive identity + report-time references | on-call (OCE-002/OQ2), behavioral (implied) | Evidenced (confirmed via git the id is absent today; enables CL-6's positive test) |
  | CL-12 | The record path must be excluded from **every** scope/stage/human-review/no-output-clean-tree computation (checklist, not one edit) | on-call (OCE-007), structural (S6), behavioral (B10) | Evidenced (T1 line 9) — largely satisfied automatically by CL-4's placement; the human-review scope pointer is the one site to verify |
  | CL-13 | `no-output-completion.md` step 2 and the recovery re-attempt Exit gain the durable no-commit-done write, ordered **durable-record-first then `state.json`** | structural (S5/S9), on-call (OCE-004) | Evidenced (review-sync F12; D13 "durable record supersedes") |
  | CL-14 | Operator doc `docs/skills/han-coding/implement-work-items.md` has 3 passages the feature inverts (lines 20/72/83) → rewrite in the same change (Definition of Done) | structural (S8), junior (DoD) | Evidenced (verbatim contradictions) |
  | CL-15 | The D20 separate hook-probe is **redundant** with the opening-entry commit → do not build it (YAGNI) | on-call (OCE-010), junior (JD-009), structural (S4) | Evidenced (simpler-version: the opening-entry commit already exercises the hook path at plan-confirmation) |
  | CL-16 | Entry fields kept minimal — no `Type` field on the no-commit-done entry (readable from the file), no dependency data in the skip entry (re-derived from the current file), no durable body-hash / fix-counter / pre-work-decision (all spec-deferred) | behavioral (B1/B4/B5) | Evidenced (D13; D21; spec Deferred) |
  | CL-17 | Fresh-vs-resume detection must **normalize the work-items path** (strip `./`, resolve `..`, no trailing slash) before comparing to the stored identity, or a valid resume is silently refused as a foreign base | behavioral (B8) | Evidenced (D10 identity is repo-root-relative path) |

- **Open Questions raised:** OQ-1 record form (→ CL-1, resolved: ledger file); OQ-2 ledger location / new tracked path acceptability (→ CL-4, resolved: inside `.implement-work-items/` via un-ignore, no new top-level path); OQ-3 grep convention trailer-vs-subject (→ CL-10, resolved: namespaced trailer); OQ-4 item id on code commits (→ CL-11, resolved: add it). All four resolved by evidence in aggregation; none escalated to the user.
- **Spec-maturity tags:** plan-level — all 17 claims. spec-level — 0 (the spec cleared iterative-plan-review; every finding is an implementation mechanic). T#-contradiction — 0 (OCE-006 affirms T1's positional-reference intent; no specialist proposed a mechanic against a committed T#). **Spec-maturity gate did not trip** (0 T#-contradictions < 2; 0 spec-level < 5).
- **Resolution source:** OQ-1..OQ-4 — evidence (Step 5 aggregation). CL-4 dispute — evidence (inside-placement is simpler and the existing exclusion demonstrably covers a tracked file under the directory).
- **Decisions produced:** D-1..D-13 (9 full: D-1 record form, D-2 run-identity trailer, D-3 decomposition, D-4 re-grounding routine, D-5 forward-reconcile AFK-only, D-6 bookkeeping-failure class, D-7 commit-boundary self-check, D-8 positional reference, D-9 item-id trailer; 4 trivial: D-10 atomic opening entry, D-11 exclusion checklist, D-12 durable-record-first write ordering, D-13 minimal entry fields). Mapping from the claim ledger: CL-1+CL-4→D-1; CL-10+CL-17→D-2; CL-2→D-3; CL-3→D-4; CL-6+CL-11→D-5 (with D-9); CL-7→D-6; CL-8→D-7; CL-9→D-8; CL-11→D-9; CL-5→D-10; CL-12→D-11; CL-13→D-12; CL-16→D-13. CL-14→Definition of Done (operator-doc rewrite); CL-15→Deferred (YAGNI) (redundant hook-probe). All 13 settled by evidence; 0 by user input.
- **Changed in plan:** all sections authored this round (the plan is R1's synthesis): Source Specification, Outcome, Context, Team Composition and Participation, Implementation Approach (Architecture and Integration Points, Data Model and Persistence, Runtime Behavior, External Interfaces), Decomposition and Sequencing, RAID Log (Risks, Assumptions), Testing Strategy, On-Call Resilience Posture, Definition of Done, Specialist Handoffs for Implementation, Deferred (YAGNI), Open Items, Summary. Security Posture and Operational Readiness omitted by judgment (no threat/infra surface).
- **Project-manager next-step recommendation:** Go to synthesis — the gate did not trip, all Open Questions resolved by evidence, no outstanding specialist handoff. One round was sufficient given the four specialists' strong convergence.
